class Customer {
  constructor(id, name, hotel) {
    this.id = id;
    this.name = name;
    this.hotel = hotel;
  }

  // use the hotel.searchGuestInData to find user

  roomServiceBreakDown(name) {
    
  }

  totalRoomServiceToday(name, date) {

  }

  overallRoomServiceTotal(name) {
  
  }

  bookingsFromGuest() {
    // instantiate new class of bookings
  }
}

export default Customer;